<Window
    x:Class="Tips.Controls.Guide"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:Tips.Controls"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    x:Name="gw"
    Title="GuideWin"
    AllowsTransparency="True"
    Background="#01FFFFFF"
    ShowInTaskbar="False"
    WindowStyle="None"
    mc:Ignorable="d">
    <Grid>
        <Border
            x:Name="border"
            BorderBrush="White"
            BorderThickness="2"
            CornerRadius="5"
            Opacity="0.8">
            <Border.Effect>
                <DropShadowEffect
                    BlurRadius="8"
                    ShadowDepth="0"
                    Color="#FF414141" />
            </Border.Effect>
            <Border
                Margin="0"
                Background="Black"
                CornerRadius="5"
                Opacity="0.5" />
        </Border>
        <Canvas x:Name="canvas" />
    </Grid>
</Window>
