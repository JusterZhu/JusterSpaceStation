<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:toolkit="http://schemas.microsoft.com/dotnet/2022/maui/toolkit"
             x:Class="MauiVideoDemo.MainPage">

    <ScrollView>
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition  />
                <RowDefinition />
            </Grid.RowDefinitions>
            <toolkit:MediaElement  x:Name="mediaElement" ShouldAutoPlay="True" ShouldShowPlaybackControls="True" Source="{Binding Url}" />
            <StackLayout Orientation="Horizontal" Grid.Row="1" HorizontalOptions="Center" >
                <Button Text="Start" Clicked="Button_Clicked" WidthRequest="100" HeightRequest="25" Margin="5" />
                <Button Text="Stop" Clicked="Button_Clicked_1" WidthRequest="100" HeightRequest="25" Margin="5"/>
            </StackLayout>
        </Grid>
    </ScrollView>
</ContentPage>
